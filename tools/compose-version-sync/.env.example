# Required: path to your stacks directory (e.g. Dockge's /opt/stacks)
STACKS_DIR=/opt/stacks

# Optional: compose filename if not the default
# COMPOSE_FILENAME=compose.yaml

# Optional: remote Docker host
# DOCKER_HOST=tcp://192.168.1.100:2375
